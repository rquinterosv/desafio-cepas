<%= form_with(model: categorywine, local: true) do |form| %>
  <% if categorywine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categorywine.errors.count, "error") %> prohibited this categorywine from being saved:</h2>

      <ul>
      <% categorywine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :wine_id %>
    <%= form.text_field :wine_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
